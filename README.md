# 사주 앱 프로젝트

React Native + Node.js + Python Flask 기반의 크로스 플랫폼 사주 모바일 앱

## 프로젝트 구조

```
사주 앱/
├── backend/                 # Node.js Express API 서버
│   ├── src/
│   │   ├── config/          # 데이터베이스, JWT 등 설정
│   │   ├── controllers/     # 비즈니스 로직
│   │   ├── middleware/      # 인증, 검증 미들웨어
│   │   ├── models/          # 데이터 모델
│   │   ├── routes/          # API 라우트
│   │   ├── utils/           # 유틸리티 함수
│   │   └── server.js        # 서버 진입점
│   └── package.json
│
├── python-saju/            # Python Flask 사주 계산 서비스
│   ├── modules/            # 사주 계산 모듈
│   ├── data/               # 만세력 데이터
│   ├── app.py              # Flask 앱 진입점
│   └── requirements.txt
│
├── mobile-app/             # React Native + Expo 앱
│   ├── src/
│   │   ├── api/            # API 통신
│   │   ├── components/     # 재사용 컴포넌트
│   │   ├── navigation/     # 네비게이션
│   │   ├── screens/        # 화면 컴포넌트
│   │   ├── store/          # Redux 상태 관리
│   │   └── utils/          # 유틸리티
│   ├── App.js
│   └── package.json
│
├── database/               # 데이터베이스 스키마 및 초기 데이터
│   ├── schema.sql          # 테이블 스키마
│   ├── init_data.sql       # 초기 데이터
│   ├── migrations/         # 마이그레이션
│   └── seeds/              # 시드 데이터
│
└── 문서/                    # 기획 및 설계 문서
    ├── 기술스택.md
    ├── DB 스키마 설계.md
    ├── 앱_페이지_구성.md
    └── ...
```

## 기술 스택

### 프론트엔드 (모바일 앱)
- React Native 0.72+
- Expo SDK 49+
- React Navigation
- Redux Toolkit
- React Native Paper

### 백엔드
- Node.js v18.x
- Express 4.18+
- MySQL/MariaDB
- JWT 인증

### 사주 계산 서비스
- Python 3.9+
- Flask 2.3+
- Korean Lunar Calendar

## 시작하기

### 1. Backend (Node.js) 실행

```bash
cd backend
npm install
cp .env.example .env
# .env 파일 수정 후
npm run dev
```

서버: http://localhost:3000

### 2. Python Flask 서비스 실행

```bash
cd python-saju
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Mac/Linux
pip install -r requirements.txt
cp .env.example .env
# .env 파일 수정 후
python app.py
```

서버: http://localhost:5000

### 3. Mobile App 실행

```bash
cd mobile-app
npm install
npm start
```

Expo 개발 서버가 시작되고 QR 코드가 표시됩니다.

### 4. 데이터베이스 설정

```bash
# MySQL 설치 후
mysql -u root -p

# 스키마 생성
source database/schema.sql

# 초기 데이터 삽입
source database/init_data.sql
```

## 개발 순서

1. ✅ 프로젝트 폴더 구조 생성
2. 📝 데이터베이스 스키마 구현
3. 📝 백엔드 API 구현
4. 📝 Python 사주 계산 로직 구현
5. 📝 모바일 앱 UI 구현
6. 📝 결제 시스템 연동
7. 🚀 테스트 및 배포

## 주요 기능

- 사주팔자 계산 (음양력 변환 지원)
- 대운, 세운 조회
- 사용자 인증 및 관리
- 사주 조회 히스토리
- 결제 시스템 (토스페이먼츠)

## 환경 요구사항

- Node.js v18.x 이상
- Python 3.9 이상
- MySQL 8.0 또는 MariaDB 10.6 이상
- Expo CLI (모바일 앱 개발)

## 문서

- [기술스택](./기술스택.md)
- [DB 스키마 설계](./DB%20스키마%20설계.md)
- [앱 페이지 구성](./앱_페이지_구성.md)
- [개발 순서 일정](./개발_순서_일정.md)

## 라이선스

작성 예정
